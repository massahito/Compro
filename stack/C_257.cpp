#include <iostream>
#include <vector>
#include <utility>
#include <string>

using namespace std;

int main()
{
	int N;
	string S;

	cin >> N;
	cin >> S;
	vector<pair<int, string>> W(N);
	for(int i = 0; i < N; i++)
	{
		int w;
		cin >> w;
		W[i].push_back()
	vector<int> C = W;

}
