#include <iostream>
#include <utility>
#include <queue>

using namespace std;

int main()
{
	int		N, M;

	cin >> N >> M;

	priority_queue<pair<int, int>>	que1;
	priority_queue<int>				que2;	
	
	for (int i = 0; i < N; i++)
		stk.push(N - 1 - i);
	for (int i = 0; i < M; i++)
	{
		int T, W, S;

		cin >> T >> W >> S;

		while(!que1.empty() && que1.top().first <= T)
		{
			que1.top().second


	

	
