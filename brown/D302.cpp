#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
	long long N, M, D;

	cin >> N >> M >> D;

	vector<pair<long long, int>> arr(N + M);

	for(int i = 0; i < N; i++)
		cin >> A[i];
	for(int  i = 0; i < M; i++)
		cin >> B[i];
	sort(A.begin(), A.end());
	sort(B.begin(), B.end());

	long long ans;
	int a = 0, b = 0;

	for(int )
}
